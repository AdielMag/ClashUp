<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>ClashUp.Tools.Dashboard</RootNamespace>
    <AssemblyName>ClashUp.Dashboard</AssemblyName>
    <!-- Local-only operator tool: never built into an image or deployed. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Google.Cloud.Compute.V1" />
    <PackageReference Include="Google.Cloud.Monitoring.V3" />
    <PackageReference Include="Google.Cloud.ArtifactRegistry.V1" />
    <PackageReference Include="Google.Apis.Auth" />
  </ItemGroup>

</Project>
